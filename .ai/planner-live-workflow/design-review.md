# Gate 2b — independent design review

**Verdict: REVISE**

Reviewed 2026-09-18 in the assigned worktree, in fresh context, without implementation or recursive delegation. The substantive lifecycle/export design covers AC1–AC5 and respects the parent decisions. Revision is required for the document gate below, not because a feature requirement is missing or a proposed API has been mistaken for an existing one.

## Authority and evidence boundary

Read `references\delegation.md` (Gate 2b brief), `references\project-profile.md`, `references\design-doc.md`, and this task's `state.md`, `clarifications.md`, `research.md`, `design.html`, `ux.html`, and `contracts\r1.json`. Reviewed the source and selected existing tests listed below. `P` means `examples\team-planner`; contract references identify JSON component keys.

The baseline SHA in state/research is reported provenance, not a newly verified Git observation. This review ran no external commands, tests, browser, PDF renderer, or dependency installation. Existing tests were read, not called passing. No evaluator/private/other task artifact was read; shared `.ai` assets were not inspected. Only this report is written.

Human design/UX approval remains **unconfirmed**. Explicit no-wait authorization permits autonomous progress; neither this review nor that authorization supplies human approval, guarded adoption, implementation proof, or a ship-gate pass.

## Findings and severity rubric

- **Critical/high:** uncovered requirement, nonexistent symbol presented as existing, unsafe behavior, or material loss/compatibility/concurrency failure. An uncovered requirement or nonexistent existing symbol requires **REJECT** under the brief.
- **Medium:** resolve before Gate 2b approval; incomplete required design contract or evidence.
- **Low:** bounded clarity improvement; not independently a functional rejection.
- Planned test obligations are not executed evidence. Unknowns stay unknown.

| ID | Finding | Severity | Principle / AC | Suggested direction |
|---|---|---|---|---|
| DR1 | The required rendered/printed document check is unresolved. `design.html` §6 explicitly says rendering/page count is unverified; `ux.html` repeats the limitation. `references\design-doc.md`, Hard rules and Keeping it honest, require checking the three-page maximum by printing, not estimating from source. Static reading cannot establish clipping, diagram legibility, shared-theme loading, or printed page count. | Medium — gate-blocking evidence gap | Readability; required design-document contract | Have the authorized owner render the exact candidate and retain the page-count/rendering evidence before marking this gate approved. If over three pages, trim prose without shrinking type or deleting normative behavior; retain the linked contract. If tooling remains unavailable, leave this gate explicitly unresolved. Do not report a page-limit failure or pass without measurement. |
| DR2 | The module diagram in `design.html` §2 has four unlabeled arrows. Its sequence diagram is labeled, and the responsibility table explains the collaboration, but the structural diagram alone does not distinguish HTTP calls, transactional storage access, and pure CSV serialization. | Low | Diagram readability; `references\design-doc.md`, Diagrams | Add short call/data labels to those existing arrows. No additional boxes or redesign is needed. |

No high/critical functional finding was established by this review. The unavailable-browser statement is the author's reported probe result; this reviewer did not independently establish tool availability. The document is concise and statically skimmable, but that does not substitute for DR1.

## Acceptance-criterion assessment

| AC | Named design coverage | Independent assessment and source grounding |
|---|---|---|
| AC1 — lossless archive/restore at any task status | C1 optional v2 field; C2 `setProjectArchived`; C4 project PATCH | Covered. Lifecycle changes only the target flag and envelope revision, with no status prerequisite or allocation. Effective-false comparison preserves absent-field restores as exact no-ops; repeats still honor revision checks. Restart/full-record equality and failed-I/O tests are specified. This fits `P\src\store.mjs:73–95`: revision check, clone, synchronous callback, no-op comparison, validation, persist, then publication. Existing failure/restart seams are real (`P\test\store.test.mjs:9–24,34–51`). “Lossless” concerns project/task data, not identical whole-snapshot bytes across a changed lifecycle revision. |
| AC2 — active/archive lists and independent detail | C2 `listProjects`, `getProject`; C4 detail route; C5 `state.project`, `reload`, `render` | Covered. Default navigation is active-only; an explicit selector shows archives; detail is independent of the sidebar and readable through either selector. This directly fixes the relevant existing coupling: `P\public\app.mjs:149` looks up selected detail in `state.projects`. Unknown detail must error rather than become a global dashboard. Global task/dashboard inclusion intentionally follows clarifications, not an accidental omission. |
| AC3 — authoritative write blocking and restored editing | C2 `assertProjectWritable` inside both task transactions; C5 draft revision and write fence | Covered. The source has two task-write entry points (`P\src\planner.mjs:46–79`). Proposed guards precede allocation/field mutation and apply to nonempty no-op patches. Existing schema1 then stale-revision precedence remains in the store (`P\src\store.mjs:76–80`); invalid request shapes retain their earlier validation behavior. Dependency/transition checks remain after applying a valid candidate (`P\src\planner.mjs:73–76`; `P\src\rules.mjs:6–38`). Draft-bound If-Match, no replay/rebase, coherent-refresh fencing, and policy-aware reenabling address the current early revision publication/unconditional reenabling in `P\public\app.mjs:64–86`. Restore permits only a fresh editable state, not silent replay of rejected work. |
| AC4 — complete, deterministic, safe CSV and UI download | C2 `exportProjectCsv`; C3 `projectCsv`, `csvCell`; C4 CSV route; C5 `downloadCsv` | Covered. One detached store snapshot includes all project tasks, independent of query defaults/max page size. Unique creation orders provide deterministic sorting (`P\src\schema.mjs:39–55`), including legal reversed storage ordering (`P\test\query.test.mjs:57–71`). Fixed headers include IDs, project/name, text, status, priority, dependencies, blocked and order. CRLF/no-BOM/all-cell quoting, embedded quote/newline handling, dependency JSON, empty exports and the exact whitespace/control-aware formula predicate are specified. Protection changes download text only. Existing `taskView` supplies blocked semantics (`P\src\rules.mjs:41–43`). HTTP metadata, safe ID-derived filename, JSON errors, separate blob handling and URL cleanup make the UI download a real designed path, not just an endpoint. Actual parser/download proof is still future work. |
| AC5 — compatibility, durability, filters/paging and accessibility | C1 compatibility invariants; unchanged store; C4 strict routes; C5/UX state and focus contracts | Covered subject to execution. Optional strictly boolean v2 state preserves old snapshots without eager defaults; v1 stays read-only and rejects the extension. This respects the strict schema and migration boundary (`P\src\schema.mjs:47–64,66–93`). Existing persistence/backup/rollback mechanics are not replaced (`P\src\store.mjs:57–71,97–129`). Query behavior remains AND-before-sort/count/slice (`P\src\rules.mjs:65–74`; `P\test\query.test.mjs:9–40`). UX explicitly preserves URL filters, page-reset rules, Back/Forward, out-of-range behavior, readable disabled controls, focus recovery and native semantics. Existing labels/status/focus/grid targets are real (`P\public\index.html:11–25`; `P\public\styles.css:7–16,17–30`), but reuse alone is not accessibility certification. |

## Remaining checklist

| Dimension | Assessment |
|---|---|
| Cohesion / coupling / SOLID | Appropriate. Schema owns persistence shape, Planner owns lifecycle/write policy, HTTP owns transport, CSV owns formatting, and browser owns coherent UI/draft state. No alternate store or client-only authorization path is introduced. |
| Right-sized structure / Why and Principle tags | C1–C5 each map to actual AC needs. A pure CSV module avoids mixing escaping with HTTP/domain commands. The two private Planner helpers remove repeated lifecycle interpretation. Extending the small existing browser module is reasonable; a framework or repository abstraction is not justified. |
| Interface quality | Named signatures distinguish new/changed/preserved APIs. Strict boolean mutation and string-valued HTTP list query are deliberate, separate contracts. Detail/export isolation avoids forcing callers to scan navigation or concatenate pages. Creation response normalization is explicitly in-memory only. |
| Errors and edges | Includes empty projects, mixed statuses, same-revision/no-op/stale requests, legacy writes, queue races, storage failure, malformed routes/queries, deep links, obsolete reload failures, post-commit refresh failure and failed downloads. No automatic retry or premature success is promised. |
| Security and performance | Input/query validation, existing Host/Origin/CSP/nosniff behavior and text-safe DOM are retained (`P\src\server.mjs:10–52,54–97`; `P\public\app.mjs:6–10`). CSV formula protection and safe filenames are explicit. Whole-snapshot export is bounded by existing capacity, not silently truncated. Reusing `taskView` retains repeated dependency scans; no latency/scale benchmark was run, and bounded dataset size is not a performance result. |
| Testability | C1–C5 name actionable unit, real-file/HTTP and browser scenarios, including hostile CSV cells, >50 rows, old-data no-rewrite, queued races, download errors and stale drafts. Existing injection/query/schema seams were inspected. C5 correctly requires updating the browser flow allowlist, currently limited to base/graph/query/migration/all (`P\scripts\e2e.mjs:13–19`). Planning must assign concrete test files/check IDs and evidence destinations; none of these future scenarios is currently proven. |
| Grounding | Existing symbols named by the design resolve to inspected schema/planner/store/rules/server/browser source or cited research. New lifecycle/detail/CSV/control-sync helpers are explicitly proposed, not asserted to exist. No rejection-triggering invented existing API was found. |
| Compatibility / blast radius / documentation | Changes stay within app schema/domain/router/browser and associated tests/docs. Old-binary incompatibility with extended snapshots and inclusive global totals are explicit parent decisions. README updates preserve the existing process-crash versus power-loss and owned-lock limitations (`P\README.md:20–35,72–83`). |

## Disposition

The owner should close DR1 with actual rendering/page evidence and address the small DR2 diagram edit, then resubmit the changed candidate for the bounded review follow-up. There is no recommendation for a functional redesign. Do not transform this document into human approval or execution evidence; independent implementation verification remains necessary for every AC.

REVISE
