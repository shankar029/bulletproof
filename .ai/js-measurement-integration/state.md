# JS measurement integration

- Starting HEAD: `3f7a77a19334fea1f4941fb186bcd88b0a121631` (observed).
- Branch: `shbs-microsoft-js-measurement-integration`.
- Full-tier continuation of accepted design, not a new design/research cycle.
- Authority: parent disposition in `.ai/workflow-reliability/state.md`,
  `continuation-plan.html`, `measurement-enablement-contracts.json`, root amendment,
  and JS execution amendment revision 3 (SHA256
  `887c8a3ba5dd9f8efd32081b3c3e20fbea8a0fada67349a1555bcda34f7b4c9e`),
  approved in `evidence/q2-js-execution-review-r2.md`.
- Human approval remains unconfirmed; explicit autonomous authorization applies.
- New task folder/live authority absent at intake. This is explicitly unadopted
  procedural implementation: manual edits have no supported guarded work action.
  No authority is cleared, admission fabricated, or guarded acceptance claimed.
- Phase: implementation, frozen final selections and independent read-only
  review/reconciliation complete. Local delivery is the commit containing this
  state and report; publication remains blocked.
- Scope: measure.py, measure_graph.py, probe.py, directly relevant tests/reference.
  Parent authority/history and examples/team-planner are read-only.
- Publication blocked by unchanged EMU403; no push/PR attempt.

## Acceptance and execution projection

1. JS-LIFE: fixed real produce/validate calls, immutable preflight/produced/final
   ownership, early immutable-base discovery and exact materialized-only archive.
2. JS-OWN: reject stale sources, config/tools/controllers, wrong-mode inventories,
   ownership/reservation/predecessor tampering and purpose swaps.
3. JS-OUTPUT: finite nonzero opaque failure policy; invalid zero/stderr rejects;
   failed receipts never become complete, including zero-JS revisions.
4. JS-REPLAY: fresh internally allocated measure-owned replay compares all semantic
   records; graph never imports measure/probe or executes a subject/tool.
5. JS-COMPAT: retain default Python behavior and native APIs; prove all-six-suffix
   graph projection and nonempty entrypoints only when integrated acceptance works.

Build order follows revision 3: graph's nonexecuting transport/ownership helpers,
measure's execution/manifests/replay, probe composition, real integration/tamper
tests, bounded independent verification and read-only review, local commit.
Reuse existing qualification fixtures; no replacement design documents.

## Limits

Scalar collectors, coverage expansion, root mutation, metric-producer admission,
process recovery, host trials, global configuration/install changes and full-root
qualification/archive cycles are out of scope and remain unavailable.
No full-quality, Q2 closure or complete original-parent-AC claim.

## Retained development evidence

The following is chronological history, not current pending state. Final-source
results supersede earlier development passes without erasing their failures.

- Initial three-method integration discovery failed in fixture setup: the new
  worktree lacked ignored TypeScript help qualification bytes. No native call
  ran. Parent supplied exact missing logs, verified against retained hashes,
  and preserved them in parent commit `13e1d309`; no requalification/install.
- `integration-first.log`: 3 methods in 534.196s, 2 passed, 1 error. The mixed
  attempt captured real base exit 0 but correctly rejected changed controller
  bytes after an in-flight local edit. This is a retained failed run, not a
  mixed pass. The zero-JS method subsequently ran two production and two fresh
  validation commands and passed. Later source changes supersede this proof.
- Python graph suite: 15 methods, 497.418s, exit 0 / OK. Source edits overlapped
  the run, so this is development feedback, not final frozen-source evidence.
- Current mixed/output run uses a frozen controller; tests/evidence only may
  change while it executes. Nonempty entrypoints remain disabled pending the
  complete positive mixed accepting path, then will be wired and tested.
- `mixed-and-output.log`: 3 methods, 416.014s, exit 0 / OK on a frozen
  controller. Mixed production and fresh validation each executed base/head
  successfully. Seven real fixture-child output cases and one real bounded
  idle failure exercised the finite output policy. These fixture-child
  commands are not passed off as the fixed native producer.
- `integrated.log`: retained failed selection, outer idle exit 124 during the
  public-probe test (which buffers nested captured output). It is not a suite
  pass or process-cleanup proof. Read-only process inspection found no matching
  integration/probe command, but absence is not proof of descendant cleanup.
  Independent verification uses fresh isolated fixtures and outer bounds
  derived from its nested finite work, without changing native idle30/max90.
- `entrypoints-tamper.log`: 5 methods, 840.570s, exit 0 / OK. Explicit
  nonempty config/suite/package entrypoints now pass real mixed production and
  accepting replay; the head-only root is absent-at-base and active-at-head.
  Rehashed alias semantics are rejected by another real fresh accepting
  replay. Rehashed predecessor/ownership/reservation and purpose tampering,
  plus source/config/controller/request changes reject. Six controller files
  remained frozen throughout. One additional, unselected output-policy test
  was appended during the run; it is not included in the five-method count.
- Independent read-only review and separate public-probe/default-compatibility
  verification are pending. Source and tests frozen until both return.
- Independent review returned one CRLF end-line defect, accepted. Real native
  reproduction is preserved in `crlf-red.log`: 1 method, 137.659s, exit 1;
  base/head native calls both exited 0, then strict Python span validation
  rejected the correctly serialized CRLF whole-source declaration.
- Correction caches line-start offsets from complete decoded source and looks
  up actual character positions with `bisect_right`. The real test additionally
  checks that an edited whole-source end-line coordinate rejects.
- Existing measure progress now includes the already-persisted execution ID
  for exact production/replay call identities in the final transcript. No
  command arguments, native bounds, environment or child-capture semantics change.
- Final frozen source pins: measure
  `5814997fc556f012cf3c10c8b1b0357a17c09ccbe738e0bd354a02611c47f8f0`,
  graph `25d2950e815d0ff7035ad08458ab045bce618a1b5c58f340ccb7cea25f7fe7e5`,
  probe `5500c70c7d7452a75580e346b51aab210e58abf810c13eec5e8ca97b3d73af83`.
  Native producer remains
  `ef4bef16abf6c04b559baa45929f49e0f92b76df4a1d30b542dbe877c0e204d5`.
  Final targeted integration and same-reviewer narrow correction review pending.
  Earlier green runs predate this correction and are not relabeled final proof.

## Final-source reconciliation

- `final-integration.log`: 9 methods, 1280.761s, exit 0 / OK. Real mixed CRLF
  entrypoints and zero-JS production/replay pass; twelve actual fixed native
  execution IDs are retained. Alias semantic tampering rejects after fresh
  replay. Ownership/predecessor/reservation/purpose/source/controller/request
  negatives and actual finite-output/timeout fixture children pass.
- `final-public-owner.log`: 3 methods, 500.634s, exit 0 / OK. Real failed native
  head producer plus matching replay archives only materialized artifacts with
  unavailable results; default Python public CLI compatibility passes twice.
  Owner-executed, not independently executed.
- `final-mode-config-corrected.log`: 2 methods, 39.367s, exit 0 / OK. Explicit
  immutable inventory/default mode, wrong-mode rejection, qualified-config
  boundary and native missing-request rejection. Earlier `final-mode-config.log`
  retains a selector typo: 1 actual method passed, 1 loader error, 42.095s.
  Existing class selector corrected; no source change.
- Fourteen distinct methods in these three passing selections; no full-suite,
  coverage, root mutation or metric completeness claim. No controller/test
  edits occurred during these final runs.
- `evidence/final-source-summary.json` binds current controller/test hashes,
  log hashes, observed statuses/durations, selectors, environment and limits.
  It is a procedural summary, not a raw receipt or guarded admission.
- Same reviewer closed CRLF including execution disposition, independently
  reconciling all final pins/logs and returning WITH-LIMITATIONS for all five
  scoped criteria. No outstanding actionable finding from that review.
  Separate verifier completed `evidence/final-public-reconciliation.json`
  without rerunning tests. It had declined final execution without an observed
  OS-temp denial; owner success and independent read-only reconciliation remain
  distinct. Current pins are not continuous independent run-time observation.
- `interfaces.md` records actual public/private seams and proposed scalar
  interfaces, including the absent lower-layer source-tool description handoff.
  No scalar helper or seventh-controller change was added.
- Compaction occurred during this longer-than-planned increment; state and actual
  touched source were re-anchored before completion. Future slices should close
  earlier instead of carrying overlapping planning/qualification context.

## Local delivery

- Final report: `report.html`; actual/proposed interface handoff: `interfaces.md`.
- Quality/probe/coverage/mutation scores unavailable; no G6 or full-quality pass.
- Git diff whitespace and final source/test/transcript hash reconciliation pass;
  Python syntax parsed for all pinned Python files without writing bytecode.
- Initial staged whitespace check caught the retained idle-failure transcript's
  trailing unittest progress space. Raw evidence was not edited. Task-local
  attributes follow parent evidence practice: preserve exact bytes across
  platforms and treat raw logs as non-diff transcripts. Source/docs remain
  subject to whitespace checks. An initial all-files exact-byte index assertion
  found normal Git CRLF-to-LF conversion for eight source/reference/test files,
  not a source edit. `evidence/git-source-pins.json` records both identities;
  comparison proved normalization-only differences and exact evidence blobs.
- Stage only this task's implementation/tests/reference and named evidence.
  Do not include parent qualification logs, parent history, Planner or eval report.
  Parent already owns qualification commit `13e1d309`; no cherry-pick here.
- No publication command is authorized. Parent receives the exact local commit
  after commit verification and integrates atop its own dependent history.
