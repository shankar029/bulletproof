probe: validated measurement configuration and original tool inputs
probe: materialized immutable base and observed head
probe: staged and rechecked exact tool artifacts
probe: collected both revision observations
probe: reconciled source, owned raw evidence and report
